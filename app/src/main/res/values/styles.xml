<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="NoShadow" parent="AppTheme">
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--style color tabs-->
    <style name="SoundCloudTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorHeight">6dp</item>
        <item name="tabTextAppearance">@style/TabCustomTextAppearance</item>
    </style>
    <!--for mapping icon in tab-->
    <style name="TabCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="PopupMenu" parent="android:Widget.PopupMenu">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/gray</item>
    </style>


    <style name="WidgetSpinner">
        <item name="android:background">@drawable/background_spinner</item>
        <item name="android:popupBackground">@drawable/bg_popup_spinner</item>
    </style>
</resources>
